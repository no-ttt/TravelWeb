.header-layer {
	display: flex;
	flex-direction: row;
	justify-content: space-between;
}

.logo-bar {
	display: flex;
	flex-direction: row;
	align-items: flex-end;
}

.logo {
	margin-left: 30px;
	margin-top: 5px;
}

.searchbar-pos {
	margin-left: 10px;
}

.navbar-pos {
	margin-top: 30px;
	margin-right: 40px;
}